import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-medusa-shop-storefront',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
